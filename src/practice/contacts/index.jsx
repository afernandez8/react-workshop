import React from 'react';
import ReactDOM from 'react-dom';
// import App from './App';

/* eslint-disable */
import('./App').then(({ default: App }) => ReactDOM.render(<App />, document.getElementById('root')));
